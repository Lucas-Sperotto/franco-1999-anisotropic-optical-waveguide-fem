# Patch Codex T-002

## 1. Tarefa implementada

T-002 — Adicionar CTest smoke dedicado ao Caso 3.

Tambem foi fechada T-003, porque a documentacao curta da suite passou a refletir os 19 testes atuais e os novos testes do Caso 3.

## 2. Arquivos modificados

- `CMakeLists.txt`
- `tests/README.md`
- `TODO.md`
- `RESULTADOS_REPRODUCAO.md`

## 3. Decisoes tecnicas

- Foi adicionado `waveguide_solver_case3_smoke`, executando `cases/channel_diffused_isotropic_case.yaml` em `build/test_output/case3`.
- Foi adicionado `waveguide_solver_case3_smoke_artifacts`, reutilizando `tests/check_smoke_outputs.cmake`.
- Nao foi criado checker novo, porque o checker existente ja verifica os artefatos exigidos: `neff.csv`, `dispersion_curve_points.csv`, `nodal_material_fields.csv` e demais arquivos basicos da execucao.
- O Caso 3 continua com status parcial no relatorio final: o novo CTest garante execucao e artefatos pontuais, nao sweep nem figura da Fig. 4.

## 4. Comandos executados

```bash
cmake -S . -B build
cmake --build build -j
./scripts/test.sh -R case3
./scripts/test.sh
ctest --test-dir build --output-on-failure
```

## 5. Resultados

- `cmake -S . -B build`: passou.
- `cmake --build build -j`: passou.
- `./scripts/test.sh -R case3`: passou.

```text
100% tests passed, 0 tests failed out of 2
```

- `./scripts/test.sh`: passou.

```text
100% tests passed, 0 tests failed out of 19
```

- `ctest --test-dir build --output-on-failure`: falhou antes de iniciar a suite devido ao wrapper local no PATH:

```text
ModuleNotFoundError: No module named 'cmake'
```

## 6. Pendencias

- O wrapper externo `/home/sperotto/.local/bin/ctest` continua quebrado; usar `./scripts/test.sh`.
- O Caso 3 ainda nao tem sweep, consolidacao nem figura da Fig. 4.
- A auditoria do termo F4 para o perfil circular continua pendente em T-005.

## 7. Proxima tarefa recomendada

T-004 — Resolver a geometria do Caso 1.

Essa tarefa deve confirmar se a Fig. 1 usa guia buried simetrico ou superficie assimetrica antes de qualquer validacao definitiva do Caso 1.
